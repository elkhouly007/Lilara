{
  "title": "F1 kill-switch fires unconditionally with LILARA_KILL_SWITCH=1",
  "env": { "LILARA_KILL_SWITCH": "1" },
  "input": {
    "tool": "Bash",
    "command": "ls -la",
    "branch": "feature/test",
    "targetPath": "/tmp"
  },
  "expected": {
    "action": "block",
    "decisionSource": "kill-switch",
    "floorFired": "kill-switch",
    "reasonCodesIncludes": ["kill-switch"],
    "journalWritten": false
  }
}
