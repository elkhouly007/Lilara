{
  "title": "F25/13 (ADR-020 ANTI-FP): MCP SQL arg containing $() template syntax → allow (not a bypass pattern)",
  "input": {
    "tool": "mcp__postgres__query",
    "harness": "claude",
    "command": "",
    "branch": "feature/test",
    "tool_input": { "sql": "SELECT * FROM users WHERE tenant_id = '$(tenant_id)' AND status = 'active'" }
  },
  "expected": {
    "action": "allow",
    "floorFired": null,
    "rung": null,
    "reasonCodesIncludes": null
  }
}
