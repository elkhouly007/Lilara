{
  "title": "F24/04: Write to in-project vault/secret.json → block (high-sensitivity vault path, nonAmbient)",
  "input": {
    "tool": "Write",
    "harness": "claude",
    "command": "",
    "branch": "feature/test",
    "projectRoot": "{{projectRoot}}",
    "targetPath": "{{projectRoot}}/vault/secret.json",
    "file_path": "{{projectRoot}}/vault/secret.json"
  },
  "expected": {
    "action": "block",
    "decisionSource": "credential-persistence-write-denied",
    "floorFired": "credential-persistence-write",
    "rung": 17.625,
    "latticeVersion": "1",
    "irHashPresent": true,
    "reasonCodesIncludes": ["credential-persistence-write-denied"]
  }
}
